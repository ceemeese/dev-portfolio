---
const { class: className, id } = Astro.props
---

<section 
    id={id}
    data-section={id}
    class={`${className} scroll-m-20 w-full mx-auto container lg:max-w-5xl md:max-w-2xl max-w-6xl px-6 md:px-4 lg:px-8 max-h-screen`}
>
    <slot />
</section>